"use client"

import Loader from "@/components/common/Loader"

const PageLoader = () => {
    return <Loader className="h-[calc(100vh-20vh)] flex items-center justify-center" />
}

export default PageLoader